# Copiar a .env y completar con los valores del proyecto de Supabase
# (Project Settings → API en el panel de supabase.com).
# .env ya está en .gitignore — nunca commitear estos valores reales.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
